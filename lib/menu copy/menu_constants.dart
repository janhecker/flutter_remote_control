/*import 'package:flutter/material.dart';

/// shows the name of the current screen
bool showSelectedMenuLabels = false;

/// shows the name of other menu screens
bool showUnselectedMenuLabels = false;

/// color when selected
const Color selectedColor = Colors.black;

/// color when not selected
const Color unselectedColor = Colors.grey;
*/
